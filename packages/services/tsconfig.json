{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "composite": true
  },
  "references": [
    { "path": "../utilities" }
  ],
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"] // Exclui arquivos que não devem ser recompilados

}
